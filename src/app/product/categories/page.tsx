import Category from '@/components/Category'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return <div className='w-3/4'>uiagsuif</div>
}

export default page